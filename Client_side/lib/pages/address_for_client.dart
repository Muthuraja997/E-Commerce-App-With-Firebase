import 'package:flutter/material.dart';

class AddressForClient extends StatelessWidget {
  const AddressForClient({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(title: const Text("Address"),),
      body:const Column(
        children: [
        ],
      ),
    );
  }
}